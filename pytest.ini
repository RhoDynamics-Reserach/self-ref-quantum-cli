[pytest]
pythonpath = src
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    ollama: tests requiring a local Ollama instance
    qpu: tests requiring physical IBM Quantum hardware
